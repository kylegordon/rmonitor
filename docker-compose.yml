services:
  rmonitor:
    build: .
    ports:
      - "8080:8080"
    environment:
      - RMONITOR_HOST=${RMONITOR_HOST:-192.168.10.24}
      - RMONITOR_PORT=${RMONITOR_PORT:-50000}
      - WEB_HOST=0.0.0.0
      - WEB_PORT=8080
    restart: unless-stopped
